# 作業ログ

事実のみを時系列で追記する。形式は `README.md` を参照。
2026-08-09 16:56 より前の記録は `001-*.md` 〜 `005-*.md`(記事形式)にある。

---

## 08-09 12:39 — 敵4種(グループ2)を実装

**実装**: ホッパー / サンドワーム / サイドカー / マザー
**数値**: ホッパーの着地硬直 30f。サンドワームは砂煙予告 f=188 → 浮上 f=232(猶予44f)、
浮上と同フレームで敵弾+3。マザーは3〜4回射出して離脱、ENEMIESプール最大12/20・敵弾5/8
**コミット**: `c9bd274`

## 08-09 12:55 — アート5種を再生成

**指示**: シルエットが読めない種を、識別点を1つに絞って作り直す
**実装**: リーパー / ホッパー / サンドワーム / サイドカー / ボマー。16クレジット消費、残408
**コミット**: `2c3cb1b`, `d44ad07`(判定サイズを縮小)

## 08-09 13:27 — グループ3、自機の可動範囲、スコアポップ廃止

**実装**: ミラージュ / チェイサー。自機の可動範囲を画面いっぱい(x 0〜144 / y 0〜128)へ。
発射回数で離脱する種に時間退場を追加。スコアポップと `SCORE_POPS` プールを削除
**コミット**: `221a084`

## 08-09 13:52 — 敵の動きに進入/巡航/離脱の語彙を入れる

**指示**: 「多くの敵がほとんど動かず、直線的に下に向かって移動するだけ。大きく横や斜め方向にも
動かして欲しい。画面のサイドに消える敵がいてもいい。一撃後に左右のどちらかに流れるように
去っていったり、八の字のように動いて離脱したり、斜め上に戻ったり」
**実装**: 11種へ遡って適用
**修正**: 画面外での消滅判定が左右を見ておらず、横へ抜ける敵が消えなかった。
未進入の個体が横へ流れると消えない経路も塞いだ
**数値**: x移動総和が0の種が3種 → 0種。横へ消える個体 30 → 229
**コミット**: `49266f1`

## 08-09 14:26 — 攻撃を持たない種にも離脱を与える

**修正**: 離脱トリガが発射しかなく、攻撃を持たない種が離脱フェーズに入れなかった。
種別テーブルのインデックス取り違えを実行時に検出する形へ
**コミット**: `9520521`

## 08-09 16:15 — 動きの方式を「傾向」から「振り付け」へ転換

**指示**: 「動きが複雑なわりによくない」。ドリフターの具体像として
「左上、右上のいずれかから、編隊を組んで登場。1秒間で画面中央あたりまで到達し、
そこで0.5秒静止してプレイヤーにむかって1撃発射。発射後は0.5秒静止して、
登場した方向とは逆の上方向にV字で戻っていく」
**実装**: 動きを時間で区切ったフェーズ列として定義する方式へ。docs/enemies.md を全面改稿
**コミット**: `cfd25a6`

## 08-09 16:19 — ドリフターを振り付け方式に(1回目・却下)

**実装**: 機ごとの時計(localFrame)で同じフェーズ列を踏ませる。STAGGER 11f
**数値**: 停止位置 (74,64)(64,50)(54,36)(44,22)、静止中の移動量0
**却下**: 停止座標から速度を逆算していたため、進入速度が機ごとに違った
(先頭 vy=1.33px/f、4機目 0.63px/f)。同じ地点から扇状にバラバラの速さで広がる
**コミット**: `706baa9`

## 08-09 16:28 — ドリフター2回目(却下)

**実装**: 速度を全機共通の定数にし、機ごとに進むフレーム数を変えた(60/52/44/36)
**修正**: 出発前も共通処理が vy を積んでおり、停止順が逆になっていた
**数値**: 進入速度 全機 (0,341) で一致。停止位置 (74,63)(74,53)(74,42)(74,31)
**却下**: 「機ごとに進む距離を変える」も全機同一スクリプトではない
**コミット**: `a737d36`

## 08-09 16:29 — 縦速度上限の適用範囲

**判断**: 「敵の縦速度はスクロールの1/2以下」は**巡航のみ**に適用。
進入(必ず止まる)と離脱(必ず去る)は対象外。振り付けの拍と両立しないため
**コミット**: `75d80f9`

## 08-09 16:39 — ドリフター3回目(採用)

**指示**: 「1体単位で、すべて同じ動きでなければならない。0.6秒かけて登場位置の反対側へ
放物線を描きながら降下。0.3秒静止し弾を撃って、0.3秒後に登場時の放物線の延長状に
画面外へ0.6秒かけて逃げる。画面外に出たら消失。この動きは1体ごとにすべて同じ。
あとは画面外に1体発生させてラン。数フレーム後に位置をずらしてもう1体発生させてラン。
これを、編隊の数ぶん繰り返す」
**実装**: 編隊という単位を実装から削除。1体分のスクリプトのみ。差は発生時刻と発生位置だけ。
放物線は縦速度に加速度22を積んで生成(三角関数不使用)。
`?only=drifter` デバッグ経路を追加
**修正**: スクリプト種がオフロード減速を受けており、路面位置で軌道が変わっていた
(相対軌道が6種類に割れる)。スクリプト種を路面減速の対象外へ
**数値**: 進入36f(vy 774→4) / 静止36f(移動量0) / 離脱36f(vy 0→−726)。
相対軌道は同一出現口の全個体で完全一致、鏡像はx符号反転で一致
**コミット**: `2de88d9`

## 08-09 16:42 — アート4種を再生成

**実装**: ハーベスタ / リーパー / サンドワーム / ボマー。占有率を数値で指定。16クレジット、残388
**数値**: ハーベスタの不透明率 94% → 56%、bbox 61×22(占有66%)。
リーパー bbox 4×11 → 6×10、サンドワーム 4×9 → 7×10、ボマー 9×10 → 8×10
**コミット**: `d1fbb18`

## 08-09 16:45 — ドリフターの降下距離と速度

**指示**: 「もう少し下(画面中央あたり)まで降下させて。スピードも少し上げて」。
あわせて「2ウェーブに移行するまでに、もう0.5秒の間がほしい。0.5から1.5の間でランダム」
**実装**: 拍(36/18/18/36)は据え置き、速度で解決。ウェーブ間の空きは基本90〜150fに30〜90fを加算
**数値**: 初速 796 → 1223(約4.8px/f)、加速度 22 → 34、降下量 55px → 86px、
停止位置y 39 → 70。ウェーブ間隔の実測 188/221/144/207/183/179/138f
**コミット**: `84b366b`

## 08-09 16:49 — 近距離発射抑止を半径8ドットの円へ

**指示**: 「近距離発射抑止を敵の当たり判定の中心から8ドットの円状に変更」
**実装**: ±32px の矩形 → `dx*dx + dy*dy <= 8*8`(pxへ落としてから整数比較。sqrt不使用)
**数値**: 中心距離 0/4/7/8px で撃たない、9/12/20px で撃つ。
自機が画面中央に居座ったときの発射率 46% → 92%。敵弾の画面内最大 4〜5発
**修正(計測)**: 1回目は自機を敵の初期位置基準で固定しており距離が保てず、
2回目は発射済みの個体を掴んでいた。発射前の個体を捕まえる形に直した
**コミット**: `9e29078`

## 08-09 16:56 — 発生位置を画面外周+16に固定

**指示**: 「画面外の配置は、画面の外周＋16ドットの位置で固定。上下から登場するならy軸はずらさない。
左右から登場するならx軸はずらさない」
**実装**: ドリフターは y=−16 固定、x のみ22px刻み。背後出現の縁も y=144 → 160 へ
**修正**: 旧方式の予備動作減速(`atkTimer` ベース)がスクリプト種にも二重に掛かっており、
個体ごとの位相差で離脱開始が最大3フレームずれ、相対軌道が2種類に割れていた。
スクリプト種を除外。yずらしを廃止して初めて表面化した
**数値**: 発生y は −16 の1種類、停止y は 67 の1種類。相対軌道は9個体すべて一致。
発射率は自機の位置によらず 100%(以前は中央で92%)
**修正(計測)**: 「発生yが−12と−16の2種類」と読みかけたが、出発後のフレームを拾っていただけ
**コミット**: `247d2ef`

## 08-09 16:58 — docs(devlog): 記事形式から時系列の追記ログへ切り替える

感想・評価を書かず、事実のみをタイムスタンプとコミットIDつきで
追記する形式にする。note記事はあとでLOG.mdを材料に清書する。
コミットIDは、その時点の状態へ戻してスクリーンショットを撮るために残す。

**コミット**: `2e9e64d`

## 08-09 17:00 — docs(devlog): ログ追記をスクリプト化し、判断と失敗も残す

指示: 「ログ追記はAIを通さずにスクリプトで追記していくくらいでok」
「AIの判断も重要。やったことはすべて、失敗も含めること」

実装: tools/devlog_sync.mjs を追加。git のコミット日時・ハッシュ・
本文を LOG.md へ機械的に流し込む。LOG.md に載っている最後のコミット
以降だけを追記するので、何度実行しても重複せず時系列も壊れない。
コミットメッセージが一次記録になる。

判断: 単純な「未記載なら追記」方式は、手書き区間より前のコミットが
末尾に付いて時系列が壊れるため採らなかった。

書式に「判断」(採った案と捨てた案)と「失敗」(却下・やり直し・計測の誤り)
の項目を追加。

**コミット**: `b4b6d5b`

## 08-09 17:00 — docs(devlog): ログ追記をスクリプト化し、判断と失敗も残す

指示: 「ログ追記はAIを通さずにスクリプトで追記していくくらいでok」
「AIの判断も重要。やったことはすべて、失敗も含めること」

実装: tools/devlog_sync.mjs を追加。git のコミット日時・ハッシュ・
本文を LOG.md へ機械的に流し込む。LOG.md に載っている最後のコミット
以降だけを追記するので、何度実行しても重複せず時系列も壊れない。
コミットメッセージが一次記録になる。

判断: 単純な「未記載なら追記」方式は、手書き区間より前のコミットが
末尾に付いて時系列が壊れるため採らなかった。

書式に「判断」(採った案と捨てた案)と「失敗」(却下・やり直し・計測の誤り)
の項目を追加。

**コミット**: `0a7badd`

## 08-09 17:02 — 展開準備: 実装済み6種を「1体分のスクリプト」のフェーズ表に落とす

**指示**: 「洗い出し済みの6項目(蛇行/ホイールソー/マザー/ホッパー/ミラージュ/リーパー)を
ドリフターと同じ「1体分のスクリプト」形式で記述するとどうなるかを、docs/enemies.md の
フェーズ表に落とし込むこと。コードは触らない」

**判断**: 6種を一律に放物線化しない。方向転換しない動きは直線のまま残す。
- リーパーは端での vx 反転2箇所を「滑らかにする」のではなく削除する案を採った。
  画面内で向きを変えるのが設計と食い違うため。滑らかな反射に直す案は捨てた
- ホイールソーは反射だけ加速度化し、降下は等速直線のまま。離脱も仕様どおり直下に据え置く
- ホッパーは頂点だけ加速度で反転させ、着地硬直は静止フェーズなので即時停止のまま残す
- スキャッターは静止フェーズを持たせない案を採った。攻撃がなく、横断を止めると役割が消えるため。
  ドリフターと同じ4拍を与える案は捨てた
- ミラージュは目標位置への加速度追従にし、上限速度で頭打ちにする。最優先で直す

**実装**: docs/enemies.md に「実装済み6種の1体分のスクリプト(展開準備)」節を追加。
各種にフェーズ表と現行実装からの変更点を書いた。コードは未着手

**数値**: 各種のフェーズ時間を明記(スキャッター 30f/60f、ホッパー 30f+30fを3回、
マザー進入90f・射出0.6秒間隔×3、ミラージュ進入60f/追従90f/離脱36f)

**コミット**: `0b5e7c6`

## 08-09 17:04 — 動きを見せる専用UIを廃止する

指示: 「動きを見せるだけの専用のUIを作るのは絶対にやめて。敵の動きチェックには、
オプションや専用のプログラムは必要ない。ゲームスタートする前のタイトル画面の背景で、
淡々と動かしてくれればいい」

削除: public-doc/motion.html, tools/build_motion_demo.mjs

判断: 専用UIは動きの定義がゲーム側と確認側で二重化する。判定API・オフロード判定・
振り付けの語彙で同じ問題を3回踏んでおり、いずれも古い方を消して解決している。
確認手段はタイトル画面のアトラクトモード(実コードのまま流す)へ一本化する。

失敗: ユーザーは以前から「タイトル画面で見せればいい」と指示していたが、
コーディネーターが ?only=drifter というデバッグ経路を作らせた。技術的な理由はなく、
早く出せることを優先した判断ミス。

修正: 仕様書の重複を解消。ドリフターの拍が旧値(60/30/30)と新値(36/18/18)の2箇所に
書かれていたため旧節を削除。「全種のフェーズ列」表も種別スクリプトと重複するため削除

**コミット**: `03f7a22`

## 08-09 17:04 — 動きを見せる専用UIを廃止する

指示: 「動きを見せるだけの専用のUIを作るのは絶対にやめて。敵の動きチェックには、
オプションや専用のプログラムは必要ない。ゲームスタートする前のタイトル画面の背景で、
淡々と動かしてくれればいい」

削除: public-doc/motion.html, tools/build_motion_demo.mjs

判断: 専用UIは動きの定義がゲーム側と確認側で二重化する。判定API・オフロード判定・
振り付けの語彙で同じ問題を3回踏んでおり、いずれも古い方を消して解決している。
確認手段はタイトル画面のアトラクトモード(実コードのまま流す)へ一本化する。

失敗: ユーザーは以前から「タイトル画面で見せればいい」と指示していたが、
コーディネーターが ?only=drifter というデバッグ経路を作らせた。技術的な理由はなく、
早く出せることを優先した判断ミス。

修正: 仕様書の重複を解消。ドリフターの拍が旧値(60/30/30)と新値(36/18/18)の2箇所に
書かれていたため旧節を削除。「全種のフェーズ列」表も種別スクリプトと重複するため削除

**コミット**: `dab80a1`

## 08-09 17:07 — タイトル画面をアトラクトにし、確認用のデバッグ経路を全部消す

**指示**: 「動きを見せるだけの専用のUIを作るのは絶対にやめて。敵の動きチェックには、
オプションや専用のプログラムは必要ない。ゲームスタートする前のタイトル画面の背景で、
淡々と動かしてくれればいい。ずっと言っているけれど、やらない理由を聞きたい」

**実装**: src/game.js の TITLE 分岐で updateAttract() を回す。本編と同じウェーブ列・
同じ敵のコードをそのまま流し、デモ専用の動きは書かない。自機は出さないので当たり判定・
スコア・残機は動かさない。狙い撃ちの目標は自機の代わりに画面下の固定点(72,128)を渡す。
描画は敵→敵弾→タイトル文字の順で重ねる。ウェーブ列を出しきったら resetEnemies() で
最初から繰り返す。START で startRun() へ入り、アトラクトの状態は破棄される。

**削除**: src/debug.js(ファイルごと)、main.js の applyDebugStart 呼び出し、
enemies.js の startAtWave / setWaveOverride / OVERRIDE_WAVE / spawnOverrideWave、
game.js の startPlayAt。URLクエリ ?wave= / ?section= / ?invuln= / ?only= は全廃。
実態と食い違った古いコメント(stages.js/enemies.js の「検証は startPlayAt 経由で行う」)も直した。

**判断**: ?wave= 等のデバッグ経路も残さず消す方を採った。アトラクトで全ウェーブが順に
流れるので確認手段として足り、残すと「ゲーム側と確認側で動きの定義が分かれる」問題が
また起きる。残す案(実測プローブが特定ウェーブから開始できて便利)は捨てた。
以後の確認はアトラクトか実プレイで行う。

**数値**: タイトル3000フレームで敵の同時最大7体・観測した個体52・敵弾のフレーム延べ1171。
START後は state 0 → 1 に遷移し敵数3で本編開始。dist-bagulas/ のデバッグ文字列は0件。
単体Web版のランタイムエラー0件

**コミット**: `1d2cb86`
